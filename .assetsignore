# Cloudflare Workers Static Assets exclude list.
# Canonical large intelligence artifacts remain in GitHub/intelligence and are intentionally
# not deployed as public static assets when they exceed Cloudflare's 25 MiB per-file limit.
intelligence/economic-graph/economic-graph.json
intelligence/brain-intelligence.json

# Git metadata is build infrastructure, not public site content.
.git/
