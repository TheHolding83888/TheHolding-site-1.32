/* Realty v2.0 artwork contract.
   High-fidelity approved-demo artwork is applied directly by realty-shell-v20.css.
   This file intentionally stays runtime-neutral so the stable v1.6 market/data logic remains the only data controller. */
