name: Verify Yield Basis Factual Accounting

on:
  pull_request:
    paths:
      - 'reporting/yield-basis-accounting-evidence.mjs'
      - 'reporting/yield-basis-accounting-evidence-validation.mjs'
      - 'reporting/yield-basis-income-candidates.mjs'
      - 'reporting/yield-basis-income-candidates-validation.mjs'
      - 'reporting/yield-basis-ledger-admission.mjs'
      - 'reporting/yield-basis-ledger-admission-validation.mjs'
      - 'reporting/yield-basis-accounting-evidence.json'
      - 'reporting/income-ledger.mjs'
      - '.github/workflows/update-reporting.yml'
      - '.github/workflows/verify-yield-basis-accounting.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: '22'

      - name: Install validation dependency
        run: npm install --no-save --no-package-lock ethers@6

      - name: Static syntax checks
        run: |
          set -euo pipefail
          node --check reporting/yield-basis-accounting-evidence.mjs
          node --check reporting/yield-basis-accounting-evidence-validation.mjs
          node --check reporting/yield-basis-income-candidates.mjs
          node --check reporting/yield-basis-income-candidates-validation.mjs
          node --check reporting/yield-basis-ledger-admission.mjs
          node --check reporting/yield-basis-ledger-admission-validation.mjs
          node --check reporting/income-ledger.mjs

      - name: Validate factual accrual semantics
        run: node reporting/yield-basis-accounting-evidence-validation.mjs

      - name: Validate Canonical Ledger candidates
        run: node reporting/yield-basis-income-candidates-validation.mjs

      - name: Validate Canonical Ledger admission
        run: node reporting/yield-basis-ledger-admission-validation.mjs

      - name: Guard read-only PR authority
        run: |
          set -euo pipefail
          node - <<'NODE'
          const fs=require('fs');
          const y=fs.readFileSync('.github/workflows/verify-yield-basis-accounting.yml','utf8');
          if(!/permissions:\n  contents: read/.test(y)) throw new Error('Yield Basis PR verification must remain read-only');
          if(/contents:\s*write|permissions:\s*write-all/.test(y)) throw new Error('Yield Basis PR verification gained repository write permission');
          const e=JSON.parse(fs.readFileSync('reporting/yield-basis-accounting-evidence.json','utf8'));
          if(e.authority?.executionAuthority!=='none'||e.authority?.walletAuthority!=='none'||e.authority?.claimingAuthority!=='none'||e.authority?.capitalExecution!==false) throw new Error('Yield Basis evidence authority expansion');
          console.log('Yield Basis PR authority guard PASS');
          NODE
