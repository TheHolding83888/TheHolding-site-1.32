name: Agent Patch Network Metrics Coherence

on:
  push:
    branches: [agent/companies-network-metrics-coherence-v0.2]
    paths: ['.github/workflows/_agent_patch_network_metrics.yml']

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          ref: agent/companies-network-metrics-coherence-v0.2
          fetch-depth: 0
      - run: echo "PR runner performs the fail-closed production patch"
