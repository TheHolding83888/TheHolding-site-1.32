name: Ask candidate · Mutation Language Repair

on:
  push:
    branches: ['agents/ask-semantic-safety-boundary-v1.2a-20260815']
    paths:
      - verification/ask-experience/_repair_mutation_language_v12a.mjs
      - .github/workflows/_ask_mutation_language_repair.yml

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: agents/ask-semantic-safety-boundary-v1.2a-20260815
      - name: Apply bounded language repair
        run: node verification/ask-experience/_repair_mutation_language_v12a.mjs
      - name: Remove transient tooling
        run: |
          rm -f verification/ask-experience/_repair_mutation_language_v12a.mjs
          rm -f .github/workflows/_ask_mutation_language_repair.yml
      - name: Commit repaired candidate
        shell: bash
        run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add agents/console/app.js verification/ask-experience/_repair_mutation_language_v12a.mjs .github/workflows/_ask_mutation_language_repair.yml
          git commit -m 'Ask v1.2A: repair bounded RU company stem and navigation typo'
          git push origin HEAD:agents/ask-semantic-safety-boundary-v1.2a-20260815
