# THE HOLDING — MASTER CONTINUITY CHECKPOINT
## 2026-08-18 09:17 (+03)
## COMPANY #010 · PASSPORT RESPONSIVE PRODUCTION + OWNER COLLABORATION MEMORY SYNC

> This is the primary continuity checkpoint after Company #010 Cypher strategy/rewards integration and the Passport productive-rate responsive UI sequence through PR #133.
>
> It is intentionally detailed because the owner explicitly asked that a new chat/model should recover both **the work** and **how to work with the owner** without repeating already-paid-for discovery and visual iteration.
>
> For changing facts, fresh live GitHub `main`, current generated artifacts and exact workflow evidence always outrank this prose checkpoint.

---

# 1. CURRENT RESUME CONTRACT

Repository:
`TheHolding83888/TheHolding-site-1.32`

Branch:
`main`

PR #133 production merge:
`b16518e30f0554e9de60063a862ba33be00319e7`

Immediately after that merge, scheduled/autonomous project updates continued moving `main`. At the start of this memory pass the fresh main head was:
`ec2303e1acd21f4854ac74f084d2211ba2d9cbd1`
(`memory: refresh current project bootstrap`)

This is normal. Never assume the merge SHA remains the current tip of `main`; verify ancestry/current bytes instead.

Fresh physical production verification after #133 confirmed `companies/company-010-public-adapter.js` on current `main` contains both:
- accepted desktop/laptop right-centered APR/APY capsule geometry;
- the new mobile two-row productive-card rule.

## Resume order for a new chat/model

1. Read live `intelligence/project-memory/CURRENT.md` from `main`.
2. Read this checkpoint.
3. Read `THE_HOLDING_BUILD_DISCIPLINE_CANON_2026-08-14.md`.
4. Read `THE_HOLDING_OWNER_COLLABORATION_OPERATING_STYLE_2026-08-18.md` for how to collaborate with the owner.
5. For Passport/layout work read `THE_HOLDING_PASSPORT_RESPONSIVE_UI_CANON_2026-08-18.md`.
6. For owner capital philosophy/strategy read `THE_HOLDING_OWNER_OPERATING_CONTEXT_2026-08-16.md` plus its latest tranches and structured owner-context JSONs.
7. Read only the live machine-readable company/subsystem artifacts needed for the current task.

Do not reread every historical checkpoint unless the current task requires archaeology.

---

# 2. PROJECT IDENTITY / HARD BOUNDARIES

The Holding is a:

**Capital Operating System + persistent intelligence, memory and governance layer for sovereign onchain companies and funds.**

Canonical loop:

`OBSERVE → REMEMBER → UNDERSTAND → REPORT → RECOMMEND → ACT → MEASURE → LEARN`

Current authority boundary remains:

**executionAuthority = none**

No:
- wallet signing;
- transaction execution;
- autonomous capital movement;
- automatic production merge/release;
- autonomous methodology mutation;
- autonomous security-critical destructive action.

Capability should grow faster than complexity. Authority should grow slower than intelligence.

---

# 3. OWNER WORKING RELATIONSHIP — IMPORTANT FOR NEW CHATS

The detailed collaboration contract now lives in:
`THE_HOLDING_OWNER_COLLABORATION_OPERATING_STYLE_2026-08-18.md`

Compact operational version:

- Default working language: Russian.
- Owner often dictates by voice; transcription may be imperfect. Resolve obvious intent from context rather than nitpicking dictation.
- Owner expects active expert judgment and orchestration, not passive restatement.
- Quality/proof is preferred over speed theatre.
- Work **one primary objective at a time** and preserve the requested sequence.
- `делай / продолжай / ок делай` authorizes implementation/branch/PR, not a new production merge.
- Every PR needs fresh explicit merge authorization (`мерджи N`).
- Owner actively reviews live production on laptop/phone and sends screenshots; visual acceptance matters.
- Once a design/engineering rule is accepted, promote it to reusable canon + verifier rather than repeating manual one-off patches.
- If desktop/laptop is accepted and the issue is mobile, do not disturb desktop while fixing mobile.
- If an assistant makes a continuity/state mistake, correct it precisely and prove the repaired state.
- Do not ask the owner to repeat known wallets, entry prices, accepted UI contracts or solved mechanisms when project memory/GitHub already contains them.

Public copy preference remains:
- concise native crypto-Twitter English when requested;
- short en dash `–`;
- use `company` rather than `portfolio` except a canonical proper name such as `Rook's portfolio`;
- avoid inflated/grandiose filler and unnecessary `we` framing.

---

# 4. MERGE / PRODUCTION GOVERNANCE — DO NOT REGRESS

Canonical interpretation:

General implementation permission is **not merge permission**.

Before every PR merge:
1. fresh fetch `main`;
2. fresh PR state;
3. exact head SHA;
4. exact base/current relationship;
5. exact-head workflow checks;
6. changed filenames;
7. immediate final refetch before merge;
8. merge using `expected_head_sha`;
9. verify physical production state appropriate to the change.

Why this matters:
- scheduled writers and autonomous memory/security refreshes move `main` frequently;
- earlier Company #010 work proved that old event SHAs and stale writer ordering can invalidate a superficially GREEN result;
- GREEN workflow is not equivalent to generated artifact physically existing in `main`.

If `main` moves, determine whether the movement is compatible and refresh proof. Never silently force stale state.

---

# 5. COMPANY #010 CYPHER — IDENTITY / BASELINE

Registry:
`010`

Name:
`Cypher`

Architecture:
`The Holding Standard`

Category:
`Bitcoin Standard`

Founding date:
owner-declared `2025-07-04`

Wallet 1:
`0xd90d1e395DE36e1e59C42F5dF537801C26BbC03f`

Wallet 2:
`0x64688F4Adc3f72CdB44d07e4879C724CD7025696`

Fluid owner-policy remains explicitly out of current Company #010 scope and is not a blocker/gap.

Performance state:
`partial-cost-basis`

Never display or infer missing Company #010 Performance as `0%`.

Execution authority:
`none`

## Owner-supplied entry prices — DO NOT ASK AGAIN

- ETH: `$2,476`
- BTC: `$73,482`
- HYPE: `$38.62`
- CVX: `$1.84`
- CRV: `$0.2280`
- AERO: `$0.60`
- LDO: `$0.8408`
- VELO: `$0.04762`

These are owner-supplied cost-basis inputs, not live market prices.

---

# 6. COMPANY #010 ACCEPTED CAPITAL / STRATEGY REPRESENTATION

## Stake DAO

Public Passport row is compacted to:
`Stake DAO · 4pool stables`

Show one total economic USD value for the strategy.
Underlying USDC / USDbC / axlUSDC / crvUSD decomposition is state/diagnostic detail and is not additive capital.

Known canonical strategy infrastructure:
- Curve Base 4pool `0xf6C5F01C7F3148891ad0e19DF78743D31E390D1f`
- RewardVault `0x6f6533b7e0730d150e617001e331ff2faa41fde4`
- Accountant `0x8f872cE018898ae7f218E5a3cE6Fe267206697F8`
- gauge `0x79edc58C471Acf2244B8f93d6f425fD06A439407`

Strategy counted once. CRV reward is not capital principal. Reference rate is bounded from the canonical productivity mechanism; exact live value is dynamic.

## GMX

Company #010 GMX positions are **GMX V2 GM positions**, not fixed BTC/ETH custody balances.

Accepted public representation:
- ETH-USDC and BTC-USDC are combined into one Passport row/window;
- row shows combined GM NAV;
- accepted label format:
  `GMX · ETH-USDC / BTC-USDC · APY <ETH rate> / <BTC rate>`
- values are dynamic and must come from live state.

Economic semantics:
- GM token NAV counted once;
- pool composition is diagnostic, not additive public capital;
- LP fee income is embedded in GM NAV;
- no separate claimable reward is created for embedded GM fee yield;
- Productivity metric is `GMX 30D Fee APY`;
- `incomeMode = embedded-in-gm-nav`;
- `claimableApplicable = false`.

Do not add a second generic APR/APY capsule to the accepted GMX compact row.

## wstETH

Aggregate capital may use ETH-equivalent accounting while Passport may show wrapper-aware quantity. Do not double count wrapper + underlying.

---

# 7. CRV FAMILY — CLOSED PRINCIPAL CLASSIFICATION, RATE STILL PARTIAL

PR #129 closed Company #010 CRV principal/custody classification.

## Direct CRV

Direct wallet CRV is a separate direct token position and is not the same economic object as the two productive wrappers below.

## Concentrator

Asset ID:
`concentrator-asdcrv`

Production-resolved snapshot at close:
- asdCRV shares: `544.000360055913`
- sdCRV underlying: `1114.674877715098`

Accounting rule:
- count the wrapper position once;
- sdCRV underlying is decomposition/redemption meaning, not additive capital.

Yield semantics:
- embedded / auto-compounded into share economics;
- public state: `Compounded`;
- current Reference APR remains `null / Pending` until the exact trailing PPS/reference gate passes.

Unknown rate must not be converted to zero.

## Convex staked cvxCRV

Asset ID:
`convex-staked-cvxcrv`

Production-resolved snapshot at close:
- total staked quantity: `972.505684662325 cvxCRV`
- direct cvxCRV: `0`
- legacy staked route: `0`
- wrapped staked route: `972.505684662325`

Capital classification:
- cvxCRV is a separate tokenized-veCRV economic asset;
- never sum cvxCRV units into CRV/sdCRV units.

Yield semantics:
- separate claimable reward streams;
- public state: `Claimable`;
- current Reference APR remains `null / Pending` because the exact active reward-stream annualisation is not fully proven under the canonical model.

Again: Pending != 0.

## Important UI correction

A false separate `veCRV` line previously appeared in Company #010 public presentation. It has been removed. Do not reintroduce a standalone veCRV position unless future live evidence proves an actual separate position.

---

# 8. veAERO / veVELO REWARDS — COMPANY-SPECIFIC LIVE PROOF

PR #130 added an exact bounded read-only Wallet 2 reader and production Rewards overlay.

This was not inferred merely by analogy from another company. Company #010 itself was read live.

First exact live proof established:

### Aerodrome
- route mode: `managed-compounded`
- public reward state: `Compounded`
- veNFT count: `1`
- first proof compounded amount: approximately `21.880640582888 AERO`
- claimable: `0`

### Velodrome
- route mode: `managed-compounded`
- public reward state: `Compounded`
- veNFT count: `1`
- first proof compounded amount: approximately `500.427242792752 VELO`
- claimable: `0`

Those quantities are historical proof snapshots and are **not current fixed values**. Fresh production data wins.

Semantic rule:
- managed compounded rewards are embedded income, not separate claimable USD accrued rewards;
- direct/mixed unresolved routes fail closed rather than fabricating a claimable amount;
- Votium remains `warming` until its exact route/value is proven;
- unknown Votium is not zero.

Production Rewards writer now runs Company #010 ve-reader before the Company #010 overlay.

---

# 9. PR #130 → #133 PRODUCT / UI EVOLUTION

This sequence contains reusable product learning and should not be rediscovered from chat history.

## PR #130 — strategy Passport + ve Rewards

Merged:
`aa1f3d79663e309c288cb8fc8ed9f249f6cfc969`

Meaning:
- removed false veCRV public row;
- kept accepted Stake DAO/GMX presentation;
- exposed CRV strategy yield states without inventing missing APR;
- integrated Company #010 veAERO/veVELO reward state;
- established production writer/verifier path.

Important post-merge distinction:
code can be in `main` while a scheduled/generated data file is still from the prior writer run. Never conflate code deployment with downstream data refresh.

## PR #131 — global productive strategy rate badges

Merged:
`051cecff803f924ca2671553a3b58a8bb0d80845`

Meaning:
- global Passport heading became exactly:
  **`Balance Sheet · Strategies`**
  with no terminal dot;
- reserve/nonproductive positions remain unbadged;
- productive positions can receive a separate APR/APY capsule derived from canonical `productivity-data.json` breakdowns;
- APR vs APY comes from source metric semantics;
- missing productive rate becomes `APR Pending` / equivalent, never 0;
- Rook's portfolio `productivityOnly` Yield Basis positions are guarded so a productive BTC/ETH mechanism is not incorrectly attached to a plain reserve BTC/ETH row;
- GMX compact APY remains its own representation and is not duplicated.

## PR #132 — accepted desktop/laptop right-centered capsules

Merged:
`920fd492570dff21f931834de7484324718906cf`

Owner reviewed the live laptop/desktop result positively.

Accepted desktop/laptop geometry:
- productive card is position-relative;
- rate capsule lives on the right;
- vertically centered in the asset card;
- visually larger/softer, inspired by Accrued Rewards token chips;
- capsule remains a separate visual object from title/quantity.

This desktop/laptop geometry is an accepted surface. Do not alter it during unrelated mobile fixes.

## PR-number continuity failure — durable lesson

After #131 was already merged, a later visual follow-up should have become #132. The assistant mistakenly replied as if #131 had just received/merged the new change. The owner immediately noticed because the site did not contain the promised change.

Permanent lesson:

**Never infer the PR number/state from conversational momentum. Fresh-check GitHub before claiming a PR identity, merge state or live result.**

This is now also preserved in the owner collaboration canon.

## PR #133 — mobile structural solution

Merged:
`b16518e30f0554e9de60063a862ba33be00319e7`

Owner mobile screenshots showed long strategy labels colliding with the right-centered APR capsules.

Rejected design approach:
- manually lower only the capsules whose current labels happen to be long;
- use per-label exceptions;
- use static text-length thresholds.

Accepted systemic solution:

**Desktop/laptop**
- retain #132 right-centered vertical-center layout unchanged.

**Mobile (`max-width: 760px`)**
Every productive card carrying a generic rate capsule becomes one reusable two-row composition:
- title = first row across full width;
- title may wrap naturally;
- value/quantity = second row left;
- APR/APY capsule = second row right;
- no overlap between future long strategy names and capsule;
- no per-strategy pixel nudges.

Physical current-main CSS verified after merge contains:
- grid columns `minmax(0,1fr) auto`;
- grid rows `auto auto`;
- title `grid-column: 1 / -1`;
- quantity bottom-left;
- badge `position: static` on mobile, bottom-right;
- accepted absolute right-center geometry remains above the mobile media rule for desktop.

Dedicated verifier:
`.github/workflows/verify-passport-rate-badge-layout.yml`

Dedicated durable design canon:
`THE_HOLDING_PASSPORT_RESPONSIVE_UI_CANON_2026-08-18.md`

README bootstrap now points future Passport responsive work to that canon.

---

# 10. PASSPORT PRODUCTIVE-RATE CANON — COMPACT FORM

Global heading:
**`Balance Sheet · Strategies`**

Reserve/nonproductive asset:
- no rate capsule.

Productive asset with exact measured rate:
- separate `APR` or `APY` capsule using canonical Productivity semantics.

Productive asset without exact measured rate:
- `Pending`, not 0.

GMX:
- accepted combined inline APY format;
- no duplicate generic badge.

Desktop/laptop:
- right side, vertical center.

Mobile:
- title row top;
- quantity/value bottom-left;
- rate capsule bottom-right.

Do not encode future strategy-name exceptions. The layout must absorb arbitrary label lengths structurally.

Special accepted compact surfaces (for example Defitea's intentionally compact Passport layout) must be regression-checked rather than globally enlarged without review.

---

# 11. WHY UI CANON + VERIFIER MATTERS

The owner explicitly asked that this type of manual correction should not recur for every new company.

Therefore the correct lifecycle is:

`owner visual feedback → general rule → global semantic implementation → verifier → GitHub-owned canon → reuse`

Not:

`owner screenshot → patch current string → forget → repeat next company`.

The same principle applies beyond UI: solved mechanisms should become reusable adapters/contracts and known mechanisms must never trigger research from zero.

---

# 12. SOURCE / ACCOUNTING DISCIPLINE THAT MUST REMAIN

Hard rules:
- `0` means measured zero;
- `null` means unknown/unavailable;
- warming/partial means insufficient proof;
- unsupported means mechanism unsupported;
- do not silently normalize null to zero.

Keep separate:
- Reference APR/Productivity;
- Embedded Yield;
- Accrued/Claimable Rewards;
- Realised Cash Flow;
- current capital/TVL/NAV;
- Treasury/liquid capital;
- Cost Basis/Invested;
- Performance;
- contribution/distribution/principal return/internal movement.

Wrapper/LP decomposition is not additive capital unless independent exposure is proven.

A claimable reward is not principal.
A compounded wrapper increase is not a claimable reward.
A Reference APR is not realised cash flow.

---

# 13. EXPENSIVE PRODUCTION LESSONS TO KEEP ACTIVE

## Writer ordering / DAG

Earlier Company #010 integration proved that two workflows triggered from the same merge event are not ordered just because they started together.

A downstream verifier can see stale upstream state if the writer is still running.

Use explicit DAG/dependency semantics rather than temporal coincidence.

## Historical workflow SHA trap

A manual rerun/recovery can check out the old triggering event SHA rather than fresh `main`.

Recovery paths that are meant to validate current production must explicitly bind fresh current state.

## Physical artifact proof

A GREEN workflow is not enough where a generated artifact is expected.

Verify:
- exact run/head;
- publish commit;
- actual file bytes in `main`;
- downstream/public consumer where relevant.

## Main moves autonomously

Security, memory and data refresh workflows can move `main` during a human PR review/merge window.

Always re-fetch immediately before merge.

---

# 14. MEMORY ARCHITECTURE — HOW A NEW CHAT SHOULD THINK ABOUT “REMEMBERING”

`CURRENT.md` is a **compact generated bootstrap**, not the entire history and not a manually curated notebook.

Current project memory model:

`Git history + Permanent Memory Vault + Decision/Learning memory + project continuity/canons + generated CURRENT bootstrap`

Use the right tier:

### Current changing fact
Read live production artifact/current subsystem state.

### Why a mechanism/design exists
Read the dedicated canon, current continuity, and Git history.

### Long-term factual observations
Use Permanent Memory Vault / system history.

### Owner strategy/thesis
Use owner operating-context files with provenance; do not convert owner thesis into market fact.

### Formal decision/outcome learning
Use Decision & Outcome Learning. Do not fake retrospective outcomes.

### Collaboration preferences
Use `THE_HOLDING_OWNER_COLLABORATION_OPERATING_STYLE_2026-08-18.md`.

The model may change. Memory belongs to The Holding.

---

# 15. WHAT THIS MEMORY PASS ADDS THAT WAS PREVIOUSLY MISSING

The repository already had strong memory for:
- architecture;
- cognitive system;
- owner investment philosophy;
- owner product vision;
- protocol/economic mechanisms;
- security incidents;
- Company #010 early production admission.

The material missing layer was **collaboration continuity**:
- how owner instructions should be interpreted;
- how much autonomy is expected in analysis/implementation;
- what requires explicit approval;
- how visual review happens;
- how accepted desktop/mobile states should be protected;
- how to react to dictation ambiguity;
- how to prevent repetitive manual UI repair;
- how to correct continuity mistakes.

That layer is now explicit and durable.

---

# 16. IMMEDIATE RESUME POINT AFTER THIS CHECKPOINT

Company #010's current major mechanics are established and public presentation has advanced through #133.

Known still-open/partial economic states include:
- Concentrator Reference APR: Pending until exact canonical trailing rate gate is satisfied;
- Convex staked cvxCRV Reference APR: Pending until exact reward-stream annualisation is fully proven;
- Votium Rewards route/value: warming;
- Company #010 Performance: partial cost basis, not complete.

These are **not invitations to jump automatically into new work**.

The current owner-directed task after #133 is to preserve continuity/memory and then continue only with the next owner request.

If the owner visually confirms the mobile Passport layout, treat the responsive productive-rate pattern as **closed/accepted and reusable**. Future company Passports should inherit the canon rather than restarting visual design from zero.

---

# 17. DO NOT DO IN A NEW CHAT

Do not:
- ask again for Company #010 wallets or owner entry prices;
- re-add false veCRV;
- treat CRV/sdCRV/cvxCRV wrapper quantities as one additive token balance;
- turn Pending APR into 0%;
- add a duplicate GMX APR badge;
- move accepted desktop capsules while fixing mobile;
- create long-label-specific CSS nudges;
- assume PR numbers from memory without checking GitHub;
- claim a scheduled writer has already published when it has not;
- research a known mechanism from zero;
- merge a new PR from a generic `делай` command;
- conflate owner thesis with market fact;
- let a screenshot override canonical economic data.

---

# 18. COMPACT HANDOFF TO THE NEXT MODEL

You are continuing The Holding after Company #010 Cypher reached a mature production integration and after the owner personally reviewed the Passport productive-rate presentation on desktop and mobile.

The owner wants the project to **compound knowledge**, not repeatedly relearn it.

Read live GitHub first. Use the latest continuity and collaboration canon. Work one objective at a time. Protect accepted surfaces. Prefer reusable semantic rules over manual patches. Maintain strict economic null/accounting discipline. Keep execution authority at none. Do not merge without fresh explicit PR authorization. Prove what is actually live.

The immediate technical/UI state after PR #133 is:

**desktop productive rate capsule = accepted right-center**

**mobile productive card = title top + value bottom-left + rate capsule bottom-right**

**reserve assets = no capsule**

**GMX = accepted compact APY row**

**unknown productive rate = Pending, never 0**

And the immediate human collaboration rule is:

**Do not make the owner teach the same solved fact, design rule or working preference twice when GitHub-owned memory can preserve it.**
