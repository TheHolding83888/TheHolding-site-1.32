# THE HOLDING — MASTER CONTINUITY
## 2026-09-01 · Realty visual asset delivery v1.4 · production-proven fast path

> **ROLE**
>
> This continuity supersedes `THE_HOLDING_MASTER_CONTINUITY_2026-09-01_REALTY_VISUAL_ASSET_HARDENING.md` as the latest Realty visual-asset resume checkpoint. The older continuity remains historical evidence of the v1.2/v1.3 transport incident.
>
> Recovery order:
>
> `CURRENT.md → this continuity → Memory Routing Index → Realty Visual Asset Delivery Canon v1.4 → exact live Realty consumer/evidence`

---

# 1. EXECUTIVE STATE

The 2026-09-01 Realty visual sequence is now production-proven across three desktop/laptop surfaces:

- upper Realty hero: new bright owner-approved asset delivered and subsequently presentation-tuned;
- second-row right `VIRTUAL REALTY` banner: brightness/overlay tuned to visual parity;
- second-row left `REAL-WORLD REALTY` banner: exact owner-supplied waterfront-villa photo delivered with composition-preserving right-biased framing and explicit owner visual acceptance.

The earlier “hero replacement PAUSED / NOT GREEN” state in the previous continuity is obsolete.

Global project authority remains unchanged; this continuity does not grant wallet/capital execution authority or autonomous methodology mutation.

---

# 2. WHY v1.4 IS A LEGITIMATE CANON VERSION

v1.3 intentionally prohibited routine version churn and allowed a new version only when a durable invariant materially changed.

The successful production sequence established such a change.

Image delivery must now be modeled as four separate planes:

1. SOURCE identity;
2. binary TRANSPORT;
3. exact CONSUMER mapping;
4. visual PRESENTATION.

This separation is durable because production proved that correct transport and consumer mapping can coexist with a visually wrong page when CSS presentation is the actual defect.

v1.4 also hardens a second durable boundary: a missing local-binary parameter in one GitHub action is not equivalent to missing GitHub repository access. Tool/permission capability must be checked precisely before reporting a blocker.

Finally, v1.4 makes exact owner attachment identity first-class: a source photo supplied for site deployment must not be replaced by a generated lookalike unless the owner explicitly requests generation/transformation.

---

# 3. AUTHORITATIVE CANON

For all new Realty image/banner/hero/card/binary delivery work, authoritative task canon is now:

`THE_HOLDING_REALTY_VISUAL_ASSET_DELIVERY_CANON_v1.4_2026-09-01.md`

v1.4 absorbs the former companion execution-prompt behavior. For a normal bounded Realty image task, the owner can provide this single canon plus the target image and instruction.

v1.3 remains historical hardened baseline evidence and must not be deleted.

---

# 4. PRODUCTION-PROVEN LESSON — HERO

The bright desktop/laptop hero asset was successfully present in repository state and referenced by the intended consumer, but the live page still appeared too dark.

The root cause was presentation, not binary transport:

- desktop image filter reduced/controlled brightness;
- a strong dark overlay covered significant portions of the hero.

Two bounded CSS-only refinements corrected visual brightness without retransmitting the image.

Durable law:

> `correct bytes + correct consumer + wrong visual → inspect PRESENTATION before TRANSPORT`

Do not restart binary upload merely because the rendered image looks dark, cropped or muted.

---

# 5. PRODUCTION-PROVEN LESSON — RIGHT VIRTUAL BANNER

The second-row right `VIRTUAL REALTY` / Metaverse card was made visually brighter through a scoped dark-theme presentation change:

- raised image brightness/saturation;
- softened the dark violet/black overlay;
- retained text readability;
- left mobile and neighboring surfaces untouched.

Durable law:

> When the asset is correct, visual parity may require only a bounded consumer-specific CSS adjustment.

---

# 6. PRODUCTION-PROVEN LESSON — LEFT REAL-WORLD BANNER

The owner supplied a specific waterfront-villa photo for the second-row left `REAL-WORLD REALTY` card.

During the interaction, an AI-generated substitute was mistakenly produced. The owner explicitly corrected the requirement: use the exact supplied photo and deploy it to the site.

The owner also superseded an earlier optional darkening request with a later instruction not to darken the image.

Production PR #570 added the immutable owner asset:

`realty/assets/owner/real-world.cab3548e.avif`

and scoped the desktop physical-card presentation so that:

- the owner asset is the effective card image;
- right-biased framing preserves the house and pushes unavoidable `cover` crop mainly toward the left;
- the image is not darkened;
- mobile remains outside the desktop override.

The owner subsequently confirmed the image was visible and correct on the live site.

Durable laws:

- exact owner attachment identity is part of the acceptance contract;
- latest explicit owner instruction supersedes older transform requests;
- minimal crop should normally be solved through focal positioning before destructive pixel crop;
- site-deployment intent must not be answered with an image-generation result.

---

# 7. GITHUB / TOOL CAPABILITY LAW

A recurring process defect was false collapse of tool limitations into generic “no GitHub access” claims.

For future tasks distinguish:

- repo read access;
- repo write/push access;
- branch/PR/merge access;
- Git object creation capability;
- direct local binary-file upload capability.

If one binary convenience primitive is missing, state that exact limitation. Do not tell the owner GitHub is unavailable when the connector and repository permissions are actually present.

Before asking the owner to perform manual GitHub work, check current connector schemas and proven exact-safe repository routes.

---

# 8. TRANSPORT LAW — NO RETURN TO BASE64 LOOPS

v1.3 transport-integrity laws remain fully inherited:

- direct exact-safe transport first;
- verified existing Git-object reuse is legal when the exact production bytes already exist;
- deterministic one-payload blob creation is legal when supported;
- manual/model-mediated large Base64 chunk transcription is forbidden;
- no branch/workflow/materializer multiplication;
- no speculative Lane B merely because direct transport feels inconvenient;
- if no exact-safe binary route exists, report `BLOCKED` instead of inventing architecture.

The successful later product work does not repeal the forensic lesson from the failed chunk/materializer sequence.

---

# 9. SIMPLE IMAGE FAST PATH

For an ordinary single-image Realty task:

1. read fresh live `main` once;
2. resolve the exact consumer and target breakpoint;
3. lock the exact owner attachment;
4. freeze crop/focal/brightness intent;
5. prepare one production asset;
6. select one exact-safe transport route from the current tool schema;
7. emit Execution Contract + `ROUTE LOCKED`;
8. create one product branch;
9. write only the asset + exact consumer/presentation change;
10. verify the bounded diff;
11. run exact-head checks;
12. merge with expected-head protection when available;
13. verify production deployment;
14. verify live visual result;
15. record owner acceptance.

No whole-repository rediscovery, old-branch archaeology, alternative-image generation or workflow invention is part of the normal path.

---

# 10. FAILURE GATE / ANTI-LOOP

After Route Lock:

- first required RED freezes writes;
- read exact error evidence;
- classify which plane failed: SOURCE / TRANSPORT / CONSUMER / PRESENTATION / DEPLOYMENT;
- preserve all locked facts not disproven;
- allow one known mechanical repair;
- second RED in the same stage = `BLOCKED`.

Do not reopen the route because of curiosity, waiting time, optimization ideas or another possible format.

---

# 11. PRODUCTION GREEN CONTRACT

A Realty image task is GREEN only after:

1. correct owner source identity;
2. correct production asset bytes;
3. correct live consumer mapping;
4. bounded product diff;
5. checks on exact head;
6. merge;
7. production deploy;
8. target breakpoint uses intended source;
9. crop/focal/brightness/overlay match owner intent;
10. out-of-scope surfaces remain correct;
11. visual acceptance is recorded.

Owner confirmation that the live site visibly shows the intended result is valid visual production acceptance evidence.

---

# 12. MEMORY ROUTING CHANGE

The hot route for Realty visual/image/binary tasks must now be:

`CURRENT → latest continuity → Memory Routing Index → THE_HOLDING_REALTY_VISUAL_ASSET_DELIVERY_CANON_v1.4_2026-09-01.md → exact live consumer/evidence`

The Router must point to v1.4.

The owner should not have to remember v1.3, companion prompts, failed transport branches or hidden recovery details.

---

# 13. VERSION BOUNDARY

Do not create v1.5 for normal image peculiarities, routine CSS balancing, provider errors, one transport rejection, or ordinary bounded repairs.

A future version requires a new durable change in:

- transport architecture/capability model;
- repository/release model;
- authority boundary;
- proof/acceptance contract.

---

# 14. RESUME SENTENCE

> **As of the 2026-09-01 v1.4 boundary, the Realty desktop/laptop hero and both second-row banners have production-proven visual updates; exact owner attachment identity, binary transport, consumer mapping and visual presentation are now separate mandatory planes; Visual Asset Delivery Canon v1.4 supersedes v1.3 for new execution and absorbs the former companion prompt; a missing local-binary action must never be misreported as missing GitHub access; manual/model-mediated Base64 chunk loops remain forbidden; correct bytes with wrong visual must trigger presentation/CSS investigation before transport restart; one product branch and bounded diff remain the normal path; and GREEN requires live visual acceptance, not only repository or CI success.**
