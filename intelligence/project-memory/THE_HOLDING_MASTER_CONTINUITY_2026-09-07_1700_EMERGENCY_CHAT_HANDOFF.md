# THE HOLDING — MASTER CONTINUITY · EMERGENCY CHAT HANDOFF
## 2026-09-07 · around 17:00Z / 20:00 MSK · owner-requested urgent checkpoint

Status: **EMERGENCY MANUAL RESUME CHECKPOINT**  
Authority: **continuity / observation / resume guidance only**  
executionAuthority: **none**

> OWNER DIRECTIVE: the current chat began hanging. The owner explicitly ordered an immediate super-detailed checkpoint so the next chat can continue without repeating context. Treat this file as the first task-specific handoff after `CURRENT.md`, but always re-read live `main` before acting because autonomous workflows may have moved the repository after this checkpoint.

---

# 0. ABSOLUTE RESUME INSTRUCTION FOR THE NEXT CHAT

Do **not** ask the owner to restate what happened in this chat.

Resume in this order:

1. Read live `intelligence/project-memory/CURRENT.md`.
2. Read this emergency handoff if CURRENT still points to it; if CURRENT points to a later checkpoint, read the later checkpoint first and use this file for the unfinished Concentrator context.
3. Read `THE_HOLDING_MEMORY_ROUTING_INDEX_v2_2026-08-26.md` only as needed for the active accounting/reporting route.
4. Re-read live `main` head and recent commits because automated writers may have moved it.
5. Re-read these exact live files before making the next change:
   - `reporting/accounting-coverage.json`
   - `reporting/accounting-coverage.mjs`
   - `reporting/concentrator-tracking-proof-validation.mjs`
   - `companies/company-010-production-state.json`
   - `.github/workflows/verify-concentrator-accounting-tracking.yml`
   - `.github/workflows/update-reporting.yml`
   - `intelligence/reliability/reporting-scheduler-workflow-definition-proof.mjs`
6. The **immediate unfinished objective** is the Concentrator/asdCRV factual-tracking status drift described in Sections 6–9 below.
7. Do not start another broad protocol, scanner, Index, Free Scan, Realty, or arbitrary-wallet feature before this last reusable accounting gap is either closed and physically materialized or proven impossible from current evidence.
8. Preserve all accounting and authority laws in Section 10.

The owner prefers concise Russian status updates while work is underway, but the implementation itself must remain production-grade, exact, systemic and well verified.

---

# 1. LIVE SOURCE BOUNDARY AT HANDOFF

Repository:
- `TheHolding83888/TheHolding-site-1.32`

Live `main` observed immediately before this emergency handoff:
- **`ac2b3c79ff90b9b113c07f0794b4c07cef22e711`**
- commit: `memory: refresh current project bootstrap`
- commit time: `2026-09-07T16:28:40Z` / `19:28:40 MSK`

Important immediate parents:
- `26e8a74900edd053d4f875a6db490b65f63e4836` — `security: extend autonomous security memory`
- `c812f7d2176b5bd480de4bc6eced77f4b44b1338` — `memory: checkpoint continuity at 4f881749`
- `4f881749d4c055bcf2c84b707c62018653cb51c2` — `data: update ICP NNS accrued rewards`
- `03ceaa70d8f0eb0f73c54df046deb54735e39073` — merged PR #678, `Accounting: probe ICP NNS rewards from direct Governance neuron info (#678)`

Latest automatic continuity before this emergency file:
- `intelligence/project-memory/THE_HOLDING_MASTER_CONTINUITY_2026-09-07_162806_AUTO_4f881749.md`
- source boundary `4f881749...`
- auto continuity commit `c812f7d2...`
- CURRENT refresh commit `ac2b3c79...`

At that automatic checkpoint:
- Accounting Coverage version: `0.10-explicit-settlement-link-accounting-mechanism-coverage-registry`
- Coverage generatedAt: `2026-09-07T15:54:16.331Z`
- mechanism count: **29**
- reusable gaps: **1**
- Canonical Income Ledger: status `partial`, generatedAt `2026-09-07T15:54:16.331Z`, observed events **716**
- Company Monthly Reports: methodology `0.4-canonical-ledger-sole-income-recognition-authority`, generatedAt `2026-09-07T15:55:14.331Z`, companies **10**
- sole highest-value reusable gap: **`concentrator_asdcrv`**, factual tracking `0/1`, reference-only `1`, known productive value reported by that Coverage artifact **$218.88**

Changing values above are resume context only. Re-read the live machine artifacts before declaring anything complete.

---

# 2. OWNER OPERATING PREFERENCES / WORKING CONTRACT RELEVANT TO THIS RESUME

Durable collaboration rules that materially affect the next step:

- Default language with owner: **Russian**.
- Owner often dictates by voice; resolve obvious transcription noise from project context instead of repeatedly asking him to restate known information.
- Prefer short/simple user-facing updates unless detail is explicitly requested.
- Work **one primary objective at a time**.
- Prefer systemic, reusable, production-grade solutions over patches.
- Preserve code stability, provenance, exact verification, security and authority boundaries.
- `UNKNOWN != 0` everywhere factual accounting is involved.
- A green workflow is not sufficient proof of completion. Expected artifacts must physically materialize on live `main` and their semantics must be verified.
- Routine low-risk repository work may proceed through verified PR + merge + production proof under the owner's existing bounded standing authorization.
- Stop for explicit owner confirmation at capital movement, wallet/signing, methodology/authority expansion, destructive/irreversible or other high-consequence boundaries.
- Current execution authority remains **none**.
- Do not build duplicate sources of truth or a second scanner.
- When the owner says **«трекай»**, the expected behavior is read-only status tracking unless he explicitly asks for changes: inspect live `main`, relevant branches/PRs, exact CI/workflows and physical artifacts; report with color markers:
  - 🟢 completed
  - 🟡 in progress + evidence-grounded approximate percentage
  - ⚪ next/planned
  - blocker/contradiction if present.

---

# 3. CANONICAL ROADMAP / WHERE THIS CHAT SITS

The current master roadmap order remains:

1. Factual Accounting Engine
2. Historical Completeness
3. Capital Flow semantics
4. Position Lifecycle
5. Discovery Layer
6. Arbitrary-address engine
7. Owner-submitted delta
8. Investor Passport
9. Free Capital Scan
10. The Holding Index / broader platform scale

Canonical pipeline rule:

`Protocol mechanism → Company Passport/position → Canonical Ledger → Historical Company Reports → Index/Scan/Passport`

Do not create a parallel scanner or parallel accounting engine.

The important change during this chat is that the broad reusable factual-tracking frontier is now down to **one** machine-identified gap: Concentrator/asdCRV for Cypher. ICP NNS was investigated separately and is now proven to be **source-limited**, not something that should be faked with APR/reference estimates.

---

# 4. WORK COMPLETED IN THIS CHAT — ICP NNS DIRECT GOVERNANCE PROBE

The prior active task was ICP NNS factual rewards / historical completeness.

## 4.1 Why the direct probe was built

The existing ICP NNS logic had public neuron identity/state coverage but factual earned-income automation depended on maturity data that the current public source did not expose. Rather than using a model/reference APR as factual income, this chat tested whether the ICP Governance canister itself could provide the missing state directly.

The probe used the canonical Governance canister:
- `rrkah-fqaaa-aaaaa-aaaaq-cai`

The read was anonymous/read-only and used Governance `get_neuron_info` style information. No signing, wallet authority, transaction, claim, stake mutation or capital movement was introduced.

## 4.2 PR #678

PR:
- **#678** — `Accounting: probe ICP NNS rewards from direct Governance neuron info`

Purpose:
- add a PR-only direct NNS Governance diagnostic probe for the canonical 41-neuron pool;
- establish whether public direct Governance state is sufficient to derive factual maturity/reward accrual;
- preserve fail-closed accounting semantics.

The diagnostic branch initially received accidental temporary placeholder/noop files during tool invocation. They were removed before final review. The final PR diff was clean and contained only the intended diagnostic probe/workflow changes.

## 4.3 Exact finding

The direct Governance probe established:

- canonical neuron set queried: **41**
- direct public neuron-info coverage: **41/41**
- ordinary maturity exposed sufficiently for factual reward accounting: **0/41**
- staked maturity exposed sufficiently for factual reward accounting: **0/41**

Interpretation:
- direct public Governance is useful for identity/state/voting coverage;
- it does **not** expose the maturity fields needed to create truthful automated factual period income for these neurons;
- therefore no automatic factual reward amount should be fabricated from APR/reference models.

This is a positive result in the epistemic sense: the system can now prove why it must remain partial/source-limited.

## 4.4 CI and merge proof

Exact-head CI for #678 completed fully green:
- **9 checks**
- no failures
- no checks still running at merge time
- the direct Governance probe step itself succeeded

Merged commit:
- **`03ceaa70d8f0eb0f73c54df046deb54735e39073`**
- merged around `2026-09-07T16:27:41Z` / `19:27:41 MSK`

Post-merge production ICP writer:
- workflow run referred to in chat as **ICP production run #91**
- result: **success**

Physical state commit:
- **`4f881749d4c055bcf2c84b707c62018653cb51c2`**
- `data: update ICP NNS accrued rewards`
- updated:
  - `companies/icp-nns-rewards-history.json`
  - `companies/icp-nns-rewards-state.json`
  - `companies/rewards-data.json`

Automatic continuity:
- workflow referred to in chat as **Continuity Checkpoint #110**
- result: **success**
- physical continuity commit: **`c812f7d2176b5bd480de4bc6eced77f4b44b1338`**
- CURRENT later refreshed at `ac2b3c79...`

## 4.5 Archaeology performed after the probe

The chat deliberately searched for any already-existing second factual maturity snapshot before asking the owner for new data.

Result:
- history of the ICP factual snapshot file showed **one** factual snapshot commit only, associated with the earlier #585 baseline;
- no later exact owner/read-only maturity snapshot was found in GitHub history;
- project/past-chat context searched in this session also did not contain a later exact maturity sum suitable for canonical factual admission.

Therefore the next honest ICP state is:

**ICP NNS = source-limited / waiting for a second exact owner/read-only maturity snapshot if historical period income is to advance.**

Do not ask the owner for that snapshot unless ICP becomes the active priority again. Do not block the current reusable accounting-gap closure on ICP.

## 4.6 What NOT to do with ICP

Do not:
- convert reference APR into factual earned income;
- infer missing maturity as zero;
- treat public voting/state coverage as reward accrual proof;
- create a second ICP scanner/data engine;
- rewrite the old #585 baseline as if it were a current snapshot.

If ICP is resumed later, start from the merged direct probe + current state/history files and seek a new exact read-only owner snapshot or a genuinely authoritative source that exposes maturity.

---

# 5. WHY THE CHAT MOVED FROM ICP TO CONCENTRATOR/asdCRV

After proving the ICP public-source limit, the chat returned to the machine-ranked reusable factual-accounting frontier.

The current Accounting Coverage artifact identified exactly one reusable gap:

- engine id: **`concentrator_asdcrv`**
- company: **Cypher / registry #010**
- protocol: **Concentrator**
- accounting family: **embedded-income**
- mechanism: **wrapper / PPS growth / auto-compounded sdCRV**
- factual tracking: **0/1** in the current materialized Coverage artifact
- factual period-event companies: **0**
- reference-only: **1**
- known productive value in the checkpointed Coverage artifact: **$218.88**

Crucially, the code to prove this mechanism was **already implemented** earlier in PR #642. This chat discovered that the current gap is not a missing engine. It is a **semantic status drift + orchestration wake-up bug**.

That is the immediate unfinished work.

---

# 6. CONCENTRATOR/asdCRV — CURRENT LIVE EVIDENCE

Relevant Company #010 file:
- `companies/company-010-production-state.json`

Observed live state during this chat:
- file version: `0.3-company-010-production-state-stakedao-complete`
- engine version: `0.4-cypher-crv-strategy-intelligence`
- generatedAt: `2026-09-07T09:52:54.608Z`
- company: `Cypher`, registry `010`
- company status: `complete-total-capital`

Current asdCRV capital row observed:
- symbol: `asdCRV`
- assetId: `concentrator-asdcrv`
- protocol: `Concentrator`
- chain: `Ethereum`
- strategy: `asdCRV · auto-compounding sdCRV`
- asdCRV shares: **544.000360055913**
- underlying symbol: `sdCRV`
- underlying quantity: **1122.513631004208**
- observed position value in that Company state generation: **$225.07**
- source: `AladdinDAO Concentrator compounder shares + convertToAssets(asdCRV→sdCRV)`
- capital rule: count asdCRV strategy NAV once; sdCRV underlying is not additive CRV capital.

Do not try to manually reconcile the `$225.07` Company-state value against the `$218.88` Coverage checkpoint value. They are different generated artifacts/times. Live machine state must be re-read at resume time.

The Company state preserves the key epistemic boundary:
- `referenceAprIsNotRealisedIncome: true`
- `unknownIsNotZero: true`
- `noDoubleCount: true`
- `noTransactions: true`
- `executionAuthority: none`

The CRV strategy provenance observed:
- CRV strategy version: `0.1-company-010-crv-strategy-intelligence`
- Concentrator resolver version: `0.2-company-010-new-mechanism-measured-stakedao-asdcrv`

---

# 7. CONCENTRATOR ROOT CAUSE FOUND — STATUS DRIFT INDIRECTLY GIVES REFERENCE APR AUTHORITY

Existing builder:
- `reporting/accounting-coverage.mjs`

Existing function:
- `concentratorAsdCrvObservationProofs(state={})`

This function already performs strong factual-tracking qualification. It checks, among other things:
- exact Company #010 / Cypher identity;
- exact CRV strategy version/status;
- execution authority = none;
- no transactions/capital movement;
- direct CRV separate from Concentrator;
- Concentrator counted once;
- cvxCRV separate asset;
- wrappers not added as CRV units;
- no double-counting;
- `UNKNOWN != 0`;
- Reference APR is not realised income;
- compounded income is not claimable reward;
- claimable reward is not principal;
- positive asdCRV share count;
- positive sdCRV underlying;
- positive NAV;
- `yield.incomeMode === auto-compounded`;
- publicStatus `Compounded`;
- claimableApplicable `false`;
- capital/share/underlying/NAV parity;
- source includes `convertToAssets`;
- Productivity parity;
- embedded-income mechanism parity;
- exact provenance / resolver version;
- valid observation month.

When all pass, it emits **tracking proof only**. It does not create a factual period-income event.

## 7.1 The problematic line

The existing qualifier requires the embedded row to satisfy:

`embeddedRow.status === 'measured'`

But the current Company state now contains:

- `status: "measured-mechanism-rate-pending"`
- `referenceAprPct: null`
- source still: `AladdinDAO SdCrvCompounder convertToAssets history`
- incomeMode still: `auto-compounded`
- claimableApplicable still: `false`

Therefore the factual tracking proof disappears solely because the **reference rate is unavailable**, even though the mechanism/current wrapper state remains measured through `convertToAssets`.

That is semantically wrong under The Holding accounting canon: Reference APR/APY must not control factual tracking authority.

## 7.2 Exact history proving the drift

The chat inspected commit:
- **`9e71d22fa12912f13fa90a40824e87600f7932bd`**
- message: `companies: refresh complete Cypher state`
- time: `2026-09-07T09:52:55Z`

Its patch showed the exact change in the embedded Concentrator row:

BEFORE:
- `status: "measured"`
- `referenceAprPct: 12.830153`

AFTER:
- `status: "measured-mechanism-rate-pending"`
- `referenceAprPct: null`

Source remained `AladdinDAO SdCrvCompounder convertToAssets history`.

This is the direct causal explanation for why an earlier PR could prove 1/1 tracking while the current Coverage artifact later regressed to 0/1.

---

# 8. EXISTING PR #642 CONTRACT — IMPORTANT CONTEXT

Merged PR:
- **#642** — `Accounting: prove Concentrator factual tracking`
- merge commit: **`e99441d9d9b9f6cd77a1b7e3b9ea8eb4171e1179`**

Original objective from PR body:
- close `concentrator_asdcrv` reusable accounting-coverage gap from persisted Company #010 factual current-state evidence;
- do not create period income;
- do not grant Reference APR accounting authority.

Expected effect stated in that PR:
- Concentrator from `0/1` → `1/1` factual tracking;
- **zero fabricated factual period events**;
- `reusableCoverageGap=false`;
- no wallet signing/claiming/capital movement/methodology authority.

Existing validator:
- `reporting/concentrator-tracking-proof-validation.mjs`

It currently starts by requiring the **live** Company state to yield one proof:
- `assert.equal(proofs.length,1,'live Concentrator current-state observation must prove factual tracking')`

It also already contains a critical negative/independence test:
- mutate reference APR in strategy/Productivity/embedded row to `999999`;
- proof must still remain `1`;
- failure message: `Reference APR leaked into Concentrator factual tracking authority`.

Other existing negative controls include:
- capital/share mismatch → 0 proofs;
- underlying mismatch → 0 proofs;
- claimable semantics drift → 0;
- embedded-income projection mismatch → 0;
- execution-capable state → 0;
- unknown resolver provenance → 0;
- unknown underlying → 0.

The current bug is subtle: the numerical APR itself is ignored, but `embeddedRow.status` changes when APR becomes unavailable. Thus Reference APR indirectly controls proof eligibility through the status enum. The fix must remove **that indirect authority**, not weaken the other gates.

Existing PR-only workflow:
- `.github/workflows/verify-concentrator-accounting-tracking.yml`

It is read-only:
- `permissions: contents: read`
- pull_request + manual only
- no push/schedule/workflow_run

Its production-shaped assertions require:
- active company count 1;
- factual tracking 1/1;
- factual event company count 0;
- reusableCoverageGap false;
- current row status `factual-tracking-no-period-event`;
- factualEventCount 0;
- factualUsdSubtotal `null`;
- source exactly Company #010 CRV Concentrator strategy;
- blocker `no-canonical-period-income-evidence` remains;
- blocker `no-factual-engine-tracking-proof` disappears;
- Concentrator removed from gapRanking;
- executionAuthority none;
- referenceMetricIsNotEarnedIncome true.

That is the correct semantic target and should be preserved.

---

# 9. SECOND ROOT CAUSE FOUND — REPORTING DOES NOT WAKE DIRECTLY ON COMPANY #010 STATE CHANGES

Canonical reporting workflow:
- `.github/workflows/update-reporting.yml`
- workflow name: `Update The Holding Reporting Data`

It has event-driven push paths for many canonical files, including rewards, stable index, embedded ledger, productivity, Defitea state, reporting builders/evidence, etc.

But at handoff it does **not** include:

`companies/company-010-production-state.json`

This means Company #010 can refresh and change a factual-tracking-relevant semantic field without directly waking canonical Reporting. Reporting may only later rebuild because another unrelated canonical input changes or via scheduled heartbeat.

This contributes to stale/misaligned Coverage state and makes the system less deterministic.

The reporting workflow is protected by:
- `intelligence/reliability/reporting-scheduler-workflow-definition-proof.mjs`

The next PR must preserve that proof and likely extend it to assert that Company #010 state is an explicit Reporting wake-up path if that file is added to `push.paths`.

Do not add a new workflow. Reuse the existing canonical Reporting workflow.

---

# 10. EXACT INTENDED FIX — NEXT CHAT SHOULD START HERE

No branch/PR for this Concentrator fix had been created yet when the owner ordered the emergency handoff. The next chat should implement it cleanly from the current live `main` after re-reading state.

## 10.1 Primary semantic fix

File:
- `reporting/accounting-coverage.mjs`

Goal:
- allow factual **tracking** proof when the wrapper mechanism/current state is measured but the **Reference APR rate is pending/unavailable**.

Safe interpretation at handoff:
- `status='measured'` = mechanism measured and reference rate currently available;
- `status='measured-mechanism-rate-pending'` = mechanism/current wrapper state measured, reference rate unavailable/pending;
- both are sufficient for current-state factual **tracking capability** if every other strong proof gate passes;
- neither status creates factual period income.

Implementation should be narrow and explicit. For example, admit an enumerated set of mechanism-measured statuses rather than deleting status validation entirely.

Do NOT allow arbitrary `unknown`, `unmeasured`, missing source/provenance, null principal/underlying, or execution-capable state.

Reference APR itself must remain completely non-authoritative.

## 10.2 Strengthen validator

File:
- `reporting/concentrator-tracking-proof-validation.mjs`

Required regression coverage:

1. Current live `measured-mechanism-rate-pending` + `referenceAprPct:null` must still produce exactly **1 tracking proof**.
2. Mutating any Reference APR value to absurd value (`999999`) must still leave **1 proof**.
3. Explicitly setting `referenceAprPct=null` while measured mechanism evidence remains valid must still leave **1 proof**.
4. Changing embedded status to a genuinely unmeasured/unknown status must produce **0 proofs**.
5. Removing or corrupting the `convertToAssets` source proof must produce **0 proofs**.
6. Existing capital/share parity, underlying parity, claimable boundary, provenance, authority and `UNKNOWN != 0` negative tests must remain.
7. No test may fabricate or admit factual period income.

## 10.3 Fix canonical Reporting wake-up

File:
- `.github/workflows/update-reporting.yml`

Add Company #010 production state to the existing `push.paths` trigger:

`companies/company-010-production-state.json`

Reason:
- accounting coverage consumes Company #010 state;
- therefore changes to that state must wake the canonical Reporting builder directly rather than waiting for unrelated inputs or the daily schedule.

Do not create another reporting workflow.

## 10.4 Extend workflow-definition proof

File:
- `intelligence/reliability/reporting-scheduler-workflow-definition-proof.mjs`

Add a deterministic assertion that the existing Reporting workflow contains the Company #010 state path if needed.

Preserve all existing scheduler, rate-continuity, canonical-ledger and authority assertions.

## 10.5 Existing Concentrator PR-only workflow

`.github/workflows/verify-concentrator-accounting-tracking.yml` already watches:
- `reporting/accounting-coverage.mjs`
- `reporting/concentrator-tracking-proof-validation.mjs`
- `companies/company-010-production-state.json`
- its own workflow/proof files

If the fix touches only the builder + validator + reporting wake-up/proof, keep this verifier read-only. Do not give it write authority.

## 10.6 Expected candidate PR shape

Likely minimal file set:
- `reporting/accounting-coverage.mjs`
- `reporting/concentrator-tracking-proof-validation.mjs`
- `.github/workflows/update-reporting.yml`
- `intelligence/reliability/reporting-scheduler-workflow-definition-proof.mjs`

Only add more files if exact live evidence proves they are necessary.

---

# 11. REQUIRED ACCEPTANCE PROOF AFTER THE FIX

A green PR is not sufficient.

Before calling Concentrator done, prove all of the following:

## Candidate / CI

- exact-head CI fully green;
- Concentrator PR-only verifier passes on the exact candidate;
- Reporting scheduler workflow-definition proof passes;
- no unexpected diff/noop/placeholder files;
- authority remains read-only/none where intended.

## Merge

- merge only after checking live `main` has not moved incompatibly;
- if main moves, rebase/update candidate as needed and re-run exact-head checks.

## Physical post-merge materialization

Wait for and verify canonical Reporting physically writes new artifacts on `main`.

Then re-read live:
- `reporting/accounting-coverage.json`
- `reporting/income-ledger.json`
- `reporting/company-monthly-reports.json` if downstream monthly materialization fires

For `concentrator_asdcrv`, expected Coverage semantics:
- activeCompanyCount = `1`
- companies = `[Cypher]`
- factualTrackingCompanyCount = **1**
- factualTrackingCompanies = `[Cypher]`
- factualEventCompanyCount = **0** unless real canonical period evidence separately exists
- reusableCoverageGap = **false**
- no `concentrator_asdcrv` entry in reusable gap ranking

Expected Cypher current-month row:
- status = `factual-tracking-no-period-event`
- factualEventCount = `0`
- factualUsdSubtotal = `null`
- factualTrackingProofSources contains only the canonical Company #010 Concentrator source
- completion blocker `no-canonical-period-income-evidence` remains
- blocker `no-factual-engine-tracking-proof` disappears

This is a key distinction:

**Closing the reusable tracking gap does NOT mean inventing asdCRV historical earned income.**

Current wrapper/PPS state proves the engine can be tracked. Factual period income still requires accepted canonical interval/history evidence.

If after materialization all 29 mechanisms have reusable tracking, the broad reusable Factual Accounting Engine coverage frontier should effectively be at **0 reusable gaps**, subject to fresh live artifact verification.

---

# 12. NON-NEGOTIABLE ACCOUNTING LAWS

These laws must survive the Concentrator fix and every later step:

1. Canonical Income Ledger is the sole factual earned-income recognition authority.
2. Reference APR/APY/reference generated income are analytics, not factual period-income authority.
3. `UNKNOWN != 0`.
4. Opening balance is baseline, not current-period income.
5. Claim/reset/withdraw/receipt after prior recognition is settlement, not second income.
6. Current claimable/state is not automatically period income.
7. Compounded wrapper growth is not a claimable reward.
8. Current wrapper state alone does not prove a historical interval's earned income.
9. Later price movement must not rewrite closed historical income.
10. Cross-family summation is forbidden unless canonical methodology explicitly admits it.
11. A tracking proof creates **tracking capability**, not income.
12. `zero period events` does not imply `no tracking capability`.
13. `GREEN workflow != physical production materialization`.
14. No wallet signing, claim, transaction, capital movement or autonomous execution authority is introduced by accounting evidence.
15. Do not silently infer $1 stablecoin pricing or current-price fallback for unsupported historical valuation.

---

# 13. RECENT FACTUAL-ACCOUNTING STATE ALREADY ACHIEVED BEFORE THIS HANDOFF

The latest automatic checkpoint shows these reusable mechanisms already covered:

- `aerodrome_veaero`: tracking **8/8**, current-month factual events 8, no reusable gap
- `velodrome_vevelo`: tracking **4/4**, current-month factual events 6, no reusable gap
- `frax_vefrax`: tracking **3/3**, current-month factual events 374, no reusable gap
- `yieldbasis_veyb`: tracking **4/4**, current-month factual events 15, no reusable gap
- `beefy_cvxcrv`: tracking **1/1**, current-month factual events 5, no reusable gap
- `convex_vlcvx`: tracking **4/4**, current-month factual events 2, no reusable gap
- `convex_staked_cvxcrv`: tracking **1/1**, current-month factual events 0, no reusable gap
- `curve_vecrv`: tracking **3/3**, current-month factual events 4, no reusable gap

Do not regress these while fixing Concentrator.

Earlier in the project, Defitea/ve33 historical accounting had already undergone multiple provenance/fail-closed hardenings, including immutable reward-token identity binding. The live Coverage/Income Ledger is now substantially further advanced than the older Sep 6 roadmap snapshot. Always use live artifacts over old percentages.

---

# 14. WHAT TO DO AFTER CONCENTRATOR IS PHYSICALLY CLOSED

Do not immediately jump to product UI.

Follow the roadmap and fresh machine evidence.

Likely next sequence:

1. Confirm reusable accounting gaps are actually zero in live Coverage.
2. Confirm no hidden production/materialization contradiction.
3. Return to **Historical Completeness**.
4. Keep ICP NNS marked source-limited unless a new exact maturity snapshot/authoritative maturity source exists.
5. Evaluate remaining historical gaps by evidence, not by protocol popularity.
6. Only after accounting/history foundation is trustworthy should the system advance toward Capital Flow semantics → Position Lifecycle → Discovery → arbitrary-address → Owner-submitted delta → Investor Passport → Free Capital Scan → Index.

Free Capital Scan remains downstream UI/product logic on top of the same canonical substrate. Do not build a second scanner.

---

# 15. IMPORTANT TEMPORAL / MATERIALIZATION NOTE

The automatic checkpoint at `c812f7d2...` had a `PRE-MATERIALIZATION WARNING` because its Accounting Coverage / Income Ledger / Monthly Reports artifacts predated the ICP diagnostic trigger boundary. This was not an ICP accounting failure; the #678 change was diagnostic and its post-merge ICP state physically materialized independently.

For the next Concentrator fix, the reporting wake-up issue is exactly why explicit Company #010 trigger wiring matters. After merge, do not assume Reporting rebuilt: inspect the run and the commit/artifact timestamps.

---

# 16. USER-FACING STATUS LANGUAGE FOR THE NEXT CHAT

The owner wants simple, compact progress reports. A useful shape after resume is:

- 🟢 ICP direct probe: done; 41/41 readable, maturity not public → source-limited, no fake income.
- 🟡 Concentrator/asdCRV: exact root cause found; status drift makes Reference APR indirectly disable factual tracking; Reporting also lacks direct Cypher-state trigger.
- ⚪ Next: narrow PR → exact CI → merge → Reporting materialization → prove `concentrator_asdcrv 1/1` with 0 fabricated period events.

Do not tell the owner broad technical detail unless he asks; keep the detail in code/checkpoint/evidence.

---

# 17. EMERGENCY HANDOFF COMPLETION STATE

At the moment the owner interrupted and asked for this checkpoint:

- No Concentrator fix PR had yet been created.
- No Concentrator fix branch should be assumed to exist; check live branches rather than guessing.
- The root cause investigation **was complete enough to implement safely**.
- The exact next coding task is described in Section 10.
- ICP #678 is merged and physically materialized.
- The existing latest auto continuity is valid background context.
- This emergency handoff is intended to prevent loss of the in-chat diagnosis and resume order.

If any later automated commit/checkpoint appears after this file, that later live state outranks numerical facts here, but the Concentrator root-cause reasoning remains relevant until code/history proves it has been fixed.

---

# 18. FINAL RESUME CONTRACT

The next model/chat should think in this hierarchy:

`CURRENT → later continuity if any → THIS EMERGENCY HANDOFF for unfinished chat state → Routing Index → live main/artifacts → exact CI/workflow evidence`

Immediate objective:

**Remove Reference APR availability from Concentrator factual-tracking authority without weakening mechanism/provenance/UNKNOWN/authority gates, wire Company #010 state into canonical Reporting wake-up, then prove physical post-merge coverage 1/1 with zero fabricated period income.**

Do not ask the owner to explain this again.

The model can change. **The memory must remain The Holding's.**
