# Temporary Unified Capital production diagnostic

This branch exists only to inspect recent canonical `unified-capital-refresh.yml` Actions runs through read-only PR CI. It must not be merged into `main`.

Latest diagnostic purpose: verify the production run triggered by merged PR #258.
